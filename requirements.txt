# [job-queue] = cài kèm APScheduler để dùng JobQueue (lập lịch nhắc việc, báo cáo)
python-telegram-bot[job-queue]==21.4
anthropic==0.117.0
python-dotenv==1.0.1

# Turso (SQLite trên mây) — chỉ dùng khi điền TURSO_DATABASE_URL vào .env
# để chạy bot luân phiên nhiều máy mà không phải copy bot.db qua lại
libsql==0.1.11

# Xuất báo cáo chi tiêu ra file Excel (.xlsx) cho lệnh /baocao
openpyxl==3.1.5
# Vẽ biểu đồ chi tiêu (PNG) kèm theo lệnh /chitieu
matplotlib==3.11.0

# Đọc tài liệu người dùng gửi (tính năng RAG): PDF và Word
pypdf==4.3.1
python-docx==1.1.2

# Chỉ dùng khi phát triển: chạy bộ test tự động bằng lệnh `pytest`
pytest==8.3.2
