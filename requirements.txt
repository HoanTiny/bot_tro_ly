# [job-queue] = cài kèm APScheduler để dùng JobQueue (lập lịch nhắc việc, báo cáo)
python-telegram-bot[job-queue]==21.4
anthropic==0.34.2
python-dotenv==1.0.1
# anthropic 0.34.2 chưa tương thích httpx >= 0.28 (httpx bỏ tham số proxies),
# nên phải ghim httpx ở 0.27.x
httpx==0.27.2

# Xuất báo cáo chi tiêu ra file Excel (.xlsx) cho lệnh /baocao
openpyxl==3.1.5

# Đọc tài liệu người dùng gửi (tính năng RAG): PDF và Word
pypdf==4.3.1
python-docx==1.1.2

# Chỉ dùng khi phát triển: chạy bộ test tự động bằng lệnh `pytest`
pytest==8.3.2
